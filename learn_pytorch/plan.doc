


![alt text](image.png)
![alt text](image-1.png)
![alt text](image-2.png)
![alt text](image-3.png)
🎯 12 月总目标（编程 + VLN）

下面是你这个月最终要做到的四件事：

① 代码：写出一个结构化的小项目（filestats + MNIST训练脚本）

要求达到：

会写类，会组织代码（package 化）

会写 CLI、用 logging

会用 pytest

会用调试工具

能写一个能训练/恢复 checkpoint 的训练脚本

② VLN：阅读 4 篇关键论文（含 R2R dataset）、理解导航任务流程

重点论文（12 月读完即可）：

R2R: Room-to-Room Vision-and-Language Navigation (CVPR 2018)

Speaker-Follower (CVPR 2018)

PREVALENT (ECCV 2020)

HAMT (NeurIPS 2021)

③ VLN 工程：跑通一个 baseline

可以选择：

VLN-CE baseline

Speaker-Follower 官方代码

Recurrent VLN baseline (R2R)

哪一个都可以，本月目标：
✔ 跑通 inference / eval
✔ 明白输入（RGB/指令）→ 输出（动作序列）

④ 形成一个“写 IROS 用的 VLN 方向初稿”

不是写论文，只是整理想法：

VLN 的挑战是什么？

我能做什么？

明年 1-2 月的复现方向是什么？

5 月投稿 IROS 怎么规划？

📅 12 月每周任务表（编程 + VLN 双线）

下面是每周“主线任务 + VLN任务 + 日常任务”，非常具体可执行。

🟦 Week1：OOP + CLI + VLN 起步（读 R2R + 跑数据）
编程主线

✔ 学习 class/继承/组合
✔ 学 argparse/logging/pathlib
✔ 完成 file_stats.py（能统计文件、输出 JSON）

🎯 产物

file_stats.py

README（如何运行）

VLN 任务

读 R2R 论文（重点）
需要理解：

输入：指令文本

输出：action sequence

视角表示（36 views）

evaluation（NE、SR、SPL）

下载 R2R 数据集（或 VLN-CE）
✓ 能完成数据解压、查看一个 sample
✓ 打印一个 instruction + panorama image

把 R2R 的数据读取写成一个 notebook
内容包含：

- 读取 JSON 指令
- 显示一条 instruction
- 显示示例路径的 3D 点


🎯 产物

一个 jupyter notebook：r2r_data.ipynb

能可视化 instruction + viewpoint 关系

🟩 Week2：package 化 + pytest + VLN 论文深入阅读（Speaker-Follower）
编程主线

✔ 把 file_stats 拆成 package
✔ 写 tests/
✔ 保证能 pytest 全部成功
✔ CLI 可以 python -m filestats.cli

🎯 产物：

filestats/ package

tests/test_core.py

pytest 截图

VLN 任务

📘 读 Speaker-Follower（重点）
要理解：

SPEAKER 自动生成指令

FOLLOWER 执行指令

Beam search 的作用

📌 写 1 页笔记：

任务输入/输出

模型结构

为什么要用 Speaker？

🎮 跑通 Speaker-Follower inference（只要 inference）
运行官方代码 example：

输入一条 instruction

输出 action sequence

得到 NE/SR/SPL

🎯 产物：

inference log

运行命令

解释结果（为什么导航失败或成功）

🟧 Week3：Debug 技巧 + VLN 工程实践（跑 baseline）
编程主线

✔ 练习 IDE 调试、pdb、breakpoint
✔ 在 filestats 中故意写一个 bug 再可视化修复
✔ 写“调试技巧笔记”一页

🎯 产物：

调试记录截图

修复后的 commit

调试笔记

VLN 任务

📌 跑通一个 VLN baseline（选择一个即可）
推荐：

VLN-CE baseline（最简单）

或 Speaker-Follower

或 PREVALENT inference

你只需要：

能运行 eval（测试一次效果）

能可视化模型走的路径

输出一条 instruction 下的预测轨迹

🎯 产物：

baseline 运行截图

输出动作序列

你自己画的 predicted trajectory（matplotlib）

🟨 Week4：MNIST 训练脚手架 + VLN 方法总结（为了 IROS）
编程主线

✔ 写 mnist_train.py
✔ 自己写 Dataset/DataLoader（或用官方）
✔ 实现 train/validate
✔ checkpoint 保存/恢复
✔ loss 连续下降

🎯 产物：

mnist_train.py

checkpoint.pth

训练日志（loss/acc）

VLN 任务：写出方向初稿（为 IROS 做准备）

这是 VLN 的关键一周。

你要写一份 1–2 页的 research idea doc：
内容包括：

① 当前 VLN 的问题（从论文看）

如：

generalization 差

long-horizon navigation 难

dataset language 过拟合

vision module 不稳定

instruction ambiguity 处理差

② 明年你的研究方向可以是什么（初步设想）

提供几个你可以做的方向：

用于 VLN 的合成数据生成（Narration/Instruction generation）

VLN 模块跨环境自适应（domain generalization）

使用 foundation model 提升 instruction grounding

路径规划强化（使用 graph policy + transformer）

③ 1~3 月你要复现的 baseline（为 IROS 做准备）

比如：

复现 HAMT（强大）

复现 PREVALENT

复现 VLN-BERT

复现 EnvDrop

任选一个。

🎯 产物：

vln_research_plan.md（1~2 页）